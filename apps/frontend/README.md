# Frontend (pendiente)

Placeholder para Next.js/React. Se puede iniciar con:

```bash
npm create next-app@latest apps/frontend --ts
```

Luego conectar a la API de `apps/backend` en `http://localhost:3001`.
