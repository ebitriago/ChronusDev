# ChronusDev Environment Variables

# JWT Secret for authentication (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=chronusdev-super-secret-key-change-in-production-min-32-chars

# Node environment
NODE_ENV=production

# Backend URL (for frontend to call backend)
# In Docker Compose: use service name (backend)
# In Coolify with separate services: use actual URL
NEXT_PUBLIC_API_URL=http://backend:3001

# Ports (optional, these are defaults)
# BACKEND_PORT=3001
# FRONTEND_PORT=3000
